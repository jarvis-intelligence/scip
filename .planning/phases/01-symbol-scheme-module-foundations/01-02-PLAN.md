---
phase: 01-symbol-scheme-module-foundations
plan: 02
type: tdd
wave: 2
depends_on: [01-01]
files_modified:
  - swift/internal/symbol/scheme.go
  - swift/internal/symbol/namer.go
  - swift/internal/symbol/namer_test.go
  - swift/internal/symbol/roundtrip_test.go
  - swift/README.md
autonomous: true
requirements: [SYM-01, SYM-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Frozen spec: every Swift symbol category in the RESEARCH mapping table (rows 1-29 where a string is defined — modules, types, nested types, funcs/methods, overloads, operators, init/deinit, getter/setter accessors, properties, lets, global vars, subscripts, enum cases, extension members, protocol requirements, witnesses/overrides, type parameters, parameters, locals, macros) has an exact golden symbol string asserted in swift/internal/symbol/namer_test.go (ROADMAP success criterion 1)"
    - "Round-trip (encoding edge): rapid-generated inputs including emoji (🚀, π, F⃗), operators (==, <->, +), backticked and multi-byte-tail names satisfy Symbol(in) → scip.ParseSymbol → scip.VerboseSymbolFormatter.FormatSymbol == identity with parsed Scheme == scip-swift; identifiers are emitted UTF-8 byte-faithful with automatic backtick escaping by the bindings formatter, and no Unicode normalization is applied (ROADMAP success criterion 1)"
    - "Extension attribution (SYM-02, adjacency edge): a member of extension Foo — same file, cross-file, or retroactive (extended type owned by another module) — produces exactly the symbol it would have if declared inside the type body, i.e. under the extended type's owner-module package and Foo# path; a retroactive method-family collision from a second declaring module disambiguates as Foo#name(+1). (ROADMAP success criterion 2)"
    - "Disambiguator boundaries (boundary edge): OverloadIndex 0 renders no disambiguator, index N greater than 0 renders (+N); getter/setter accessors and a same-named zero-arg method form one ordering group resolved by that index (RESEARCH rows 10/14/15)"
    - "Empty input (empty edge): SymbolInput with an empty Module or an empty Name returns an error and emits no symbol string; an empty ContainerPath is valid and means a top-level symbol"
    - "Determinism (ordering + precision edges): overload indices and local ordinals derive from source declaration order only; Symbol and LocalSymbol are pure functions of their inputs — identical input yields the identical string on every call and under parallel calls (concurrency edge); OverloadIndex renders exact integers with no cap, rounding, or tie-break beyond source position"
    - "Locals: LocalSymbol(sourceName, ordinal) sanitizes to a simple-identifier (non-identifier characters collapse to _, ordinal N greater than 0 appends _N) and round-trips through scip.ParseSymbol as the local form with scip.IsLocalSymbol true — Unicode source names (🚀, CJK) never appear raw in a local id (RESEARCH row 28)"
    - "The DeclKind → scip.SymbolInformation Kind mapping is frozen in scheme.go (one ScipKind method used by both future indexing paths) covering at minimum: Module=29, Struct=49, Class=7 (also actor), Enum=11, Protocol=42, TypeAlias=55, Function=17, Method=26, Operator=34, Constructor=9 (init), Getter=18, Setter=45, Property=41, Constant=8, Variable=61, Subscript=47, EnumMember=12, ProtocolMethod=68, TypeParameter=58, Parameter=37, Macro=25"
    - "scheme.go's package doc comment states the three frozen-spec extras verbatim from RESEARCH (determinism rule; canonical #if policy = semantic path indexes the canonical build configuration — macOS arm64, host Swift version, DEBUG — and the fallback selects the same single canonical branch; dual-path identity rule), and swift/README.md carries a Symbol scheme section restating them"
  prohibitions:
    - statement: "MUST NOT emit two different symbol strings for the same declaration depending on which indexing path (semantic vs fallback) produced it — dual-path identity is the navigation-trust contract; silent divergence would corrupt goToDefinition/findReferences"
      status: unverified
      flagged: true
    - statement: "MUST NOT build symbol strings outside the namer's *scip.Symbol + VerboseSymbolFormatter path (no fmt.Sprintf/Sprintf-style descriptor concatenation anywhere in swift/), and MUST NOT change the frozen scheme constants or mapping rules after this phase without a recorded decision — downstream goldens and stored indexes depend on frozen strings"
      status: unverified
      flagged: true
  artifacts:
    - swift/internal/symbol/scheme.go
    - swift/internal/symbol/namer.go
    - swift/internal/symbol/namer_test.go
    - swift/internal/symbol/roundtrip_test.go
    - swift/README.md (Symbol scheme section)
  key_links:
    - "swift/internal/symbol/namer.go -> scip.Symbol / scip.VerboseSymbolFormatter / scip.ParseSymbol (build → format → validate pipeline)"
    - "swift/internal/symbol/scheme.go DeclKind.ScipKind -> scip.SymbolInformation Kind constants (single kind-mapping source for Phase 2 fallback and Phase 3 semantic paths)"
    - "swift/internal/symbol golden tests -> RESEARCH Symbol Scheme Mapping Table rows 1-29 (normative spec skeleton)"
  flagged_assumptions:
    - "ASSUMED (RESEARCH gap 3): accessor symbols are prescribed Ruby-style — getter reuses the property's Method-shaped descriptor with Kind Getter, setter is the backtick-escaped `name=`() method with Kind Setter; collision with a same-named zero-arg method resolves via the shared overload index. Alternative (defer accessor symbols) was rejected to keep accessor navigation in v1."
    - "ASSUMED (RESEARCH gap 4): retroactive extension members are attributed to the OWNER module's package (the module owning the extended type), not the declaring module — chosen because SYM-02's findReferences/typeHierarchy rationale requires the member to live under the extended type's full identity."
    - "LIMITATION (verified against bindings/go/scip/symbol_formatter.go:100-126): the grammar/formatter supports disambiguators only on Method descriptors — retroactive Term-family (property/let/case) same-name collisions across declaring modules cannot carry (+N). Phase 1 freezes: Method-family retroactive collisions disambiguate via (+N); Term-family collisions are a documented known limitation in scheme.go, to be resolved with Phase-3 USR evidence if real-world cases appear (recorded fallback: synthetic simple-identifier suffix)."
---

<objective>
Freeze the Swift SCIP symbol scheme as executable spec + shared namer: a golden table covering every mapping-table row, rapid property round-trip tests through scip.ParseSymbol/FormatSymbol, extension attribution under the extended type's path (SYM-02), and local-symbol sanitization — all in swift/internal/symbol, built test-first.

Purpose: The symbol scheme is the vocabulary both indexing paths speak (Phase 2 fallback, Phase 3 semantic). Freezing it now with round-trip proofs prevents every later phase from mis-naming symbols; extension attribution is the single most common mis-attribution bug in Swift indexers.

Output: scheme.go (frozen constants + DeclKind/Kind mapping + normative doc), expanded namer.go (Symbol, LocalSymbol over the full SymbolInput space), namer_test.go (golden table), roundtrip_test.go (rapid properties), README Symbol-scheme section.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-symbol-scheme-module-foundations/01-RESEARCH.md
@.planning/phases/01-symbol-scheme-module-foundations/01-PATTERNS.md
@.planning/phases/01-symbol-scheme-module-foundations/01-01-SUMMARY.md
@swift/internal/symbol/namer.go
@swift/internal/symbol/namer_test.go
@bindings/go/scip/symbol_formatter.go
@bindings/go/scip/symbol_parser.go
@bindings/go/scip/symbol_test.go
@bindings/go/scip/position_test.go
</context>

<feature>
  <name>Frozen Swift SCIP symbol scheme + shared namer with round-trip proofs</name>
  <files>swift/internal/symbol/scheme.go, swift/internal/symbol/namer.go, swift/internal/symbol/namer_test.go, swift/internal/symbol/roundtrip_test.go</files>
  <behavior>
    Golden table (namer_test.go) — input → expected exact string, one case per RESEARCH mapping row (module M = "MyMod" in tests where a module name is needed):
    - Row 1 module: Kind Module, Name "M" → "scip-swift swiftpm M . M/"
    - Row 2 system module: IsSystemModule true, Module "Swift", SwiftToolchainVersion "6.2.4" → "scip-swift swift Swift 6.2.4 Swift/"
    - Rows 3-6 top-level types: struct Shape → "scip-swift swiftpm M . Shape#"; enum Color → "… . Color#"; protocol Drawable → "… . Drawable#"; typealias FooAlias → "… . FooAlias#"
    - Row 7 nested type: ContainerPath [Outer(Struct), Inner(Struct)] → "scip-swift swiftpm M . Outer#Inner#"
    - Row 8 top-level func parse → "scip-swift swiftpm M . parse()."
    - Row 9 method: ContainerPath [Shape(Struct)] Name area Kind Method → "scip-swift swiftpm M . Shape#area()."
    - Row 10 overloads: resize OverloadIndex 0/1/2 → "Shape#resize().", "Shape#resize(+1).", "Shape#resize(+2)."
    - Row 11 operators: Name "+" on Vec → "scip-swift swiftpm M . Vec#+()." (no escaping; + is an identifier character); Name "==" → "scip-swift swiftpm M . Vec#`==`()." (formatter backtick-escapes)
    - Row 12 init: Name init Kind Constructor idx 0/1 → "Vec#init().", "Vec#init(+1)."
    - Row 13 deinit: "Vec#deinit()."
    - Row 14 getter: Kind Getter, property area → "Shape#area()."
    - Row 15 setter: Kind Setter → "scip-swift swiftpm M . Shape#`area=`()."
    - Rows 16-18 terms: property origin → "Shape#origin."; member let origin → "… . origin."; global var config → "… . config."
    - Row 19 subscript idx 0/1: "Vec#subscript().", "Vec#subscript(+1)."
    - Row 20 enum case: "Color#red."
    - Row 21 extension member (same module, any file — SYM-02): identical input shape to row 9 → "scip-swift swiftpm M . Shape#area2()." — the namer API has no extension flag; ContainerPath is the extended type's ancestry
    - Row 22 retroactive extension (SYM-02): member declared in module Utils on Shape owned by module App → caller passes Module "App" → "scip-swift swiftpm App . Shape#spike()."; second same-named retroactive method from another module → "scip-swift swiftpm App . Shape#spike(+1)."
    - Row 23 protocol requirement method: Kind ProtocolMethod → "Drawable#draw()."
    - Rows 24-25 witness/override: own paths "Circle#draw().", "Dog#speak()." (relationship edges are Phase 4; paths frozen now)
    - Row 26 type parameter: ContainerPath [Box(Struct)], Name "T", Kind TypeParameter → "scip-swift swiftpm M . Box#[T]" (descriptors chain without separator — verified in-repo shape "…method(+1).(param)[TypeParam]" at bindings/go/scip/symbol_test.go:50)
    - Row 27 parameter: ContainerPath [f(Func)], Name "x", Kind Parameter → "scip-swift swiftpm M . f().(x)"
    - Row 28 locals: LocalSymbol("i", 0) → "local i"; LocalSymbol("count", 2) → "local count_2"; LocalSymbol("🚀", 0) → "local _" (sanitized); LocalSymbol("x", 1) → "local x_1"
    - Row 29 macro: Name "Preview" Kind Macro → "scip-swift swiftpm M . Preview!"
    - Rows 30-31 (property wrappers, self/Self) emit no string — documented in scheme.go only
    Error cases: SymbolInput{Module: ""} → non-nil error; SymbolInput{Name: ""} → non-nil error; IsSystemModule true with empty SwiftToolchainVersion → version renders "." (deterministic placeholder)
    Properties (roundtrip_test.go, rapid):
    - TestNamerRoundTrip: generated inputs (modules incl "🄼odule", containers 0-3 deep, names from "foo","init","`==`","🚀","π","F⃗","back``tick","$0","a-b","_x","<->","==", kinds sampled across the DeclKind set, OverloadIndex 0-5) → Symbol → ParseSymbol → FormatSymbol == identity, Scheme == "scip-swift"
    - TestLocalSymbolRoundTrip: generated source names incl Unicode tails → IsLocalSymbol true, ParseSymbol succeeds, FormatSymbol identity
    - TestNamerPure: calling Symbol twice on the same input returns byte-identical strings (determinism)
  </behavior>
  <implementation>
  scheme.go holds only frozen constants (Scheme "scip-swift", ManagerSwiftPM "swiftpm", ManagerSystem "swift"), the DeclKind enum with one value per mapping row family, the ScipKind() mapping method returning scip.SymbolInformation Kind constants (verify exact generated constant names in bindings/go/scip/scip.pb.go), and the normative package doc (grammar summary, determinism rule, canonical #if policy, dual-path identity rule, the Term-collision limitation). namer.go builds *scip.Symbol values — Descriptor suffixes via Descriptor_Namespace/Type/Term/Method/TypeParameter/Parameter/Macro (never the deprecated Descriptor_Package alias), OverloadIndex greater than 0 mapped to the Method Disambiguator string "+"+N, system-module packages using ManagerSystem + SwiftToolchainVersion, SwiftPM packages using Version "." — formats via scip.VerboseSymbolFormatter.FormatSymbol, and validates via scip.ParseSymbol before returning. LocalSymbol sanitizes to simple-identifier characters and appends _N for ordinal greater than 0. No fmt.Sprintf of descriptor text anywhere.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — golden table + rapid round-trip property tests against the tracer namer</name>
  <read_first>
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-RESEARCH.md (sections "Namer API shape (plan 01-02)", "Symbol Scheme Mapping Table" rows 1-31 + "Frozen-spec extras", "Code Examples" rapid round-trip test, "Common Pitfalls" rows 1-6)
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-PATTERNS.md items 4 (scheme.go comment discipline), 6 (golden table shape, symbol_test.go:10-28 + 125-134), 7 (rapid generator pattern, position_test.go:64-101 + sort_test.go:129-158), Shared Patterns "Test conventions"
  - /Users/ddphuong/Projects/jarvis-ai/scip/bindings/go/scip/symbol_test.go lines 10-55 (table + subtest + cmp.Diff idioms)
  - /Users/ddphuong/Projects/jarvis-ai/scip/bindings/go/scip/position_test.go lines 64-101 (rapid.Custom/Draw/Check skeleton)
  - /Users/ddphuong/Projects/jarvis-ai/scip/bindings/go/scip/symbol_formatter.go lines 100-126 (suffix switch — confirms disambiguators render only for Descriptor_Method)
  - /Users/ddphuong/Projects/jarvis-ai/scip/swift/internal/symbol/namer_test.go (tracer test from 01-01 — extend, do not delete)
  </read_first>
  <acceptance_criteria>
  - swift/internal/symbol/namer_test.go declares a table test (anonymous type test struct inside the test func, cases keyed by t.Run on the expected string) covering every golden case in the feature behavior block — at minimum the rows for module, system module, struct/enum/protocol/typealias, nested type, top-level func, method, overloads 0/1/2, both operator forms, init(+0/+1), deinit, getter, escaped setter, property/let/global-var terms, subscript(+0/+1), enum case, extension member, retroactive owner-module attribution + method-family (+1) collision, protocol method, witness/override own paths, type parameter, parameter, and macro
  - swift/internal/symbol/namer_test.go contains error-case tests: empty Module and empty Name each return a non-nil error
  - swift/internal/symbol/namer_test.go contains LocalSymbol cases: ("i",0)→"local i", ("count",2)→"local count_2", ("🚀",0)→"local _", ("x",1)→"local x_1"
  - swift/internal/symbol/roundtrip_test.go declares genName/genContainer/genInput generators (rapid.SampledFrom / rapid.SliceOfN / rapid.IntRange / rapid.Custom, names biased to operators, emoji, backticks, multi-byte tails) and tests TestNamerRoundTrip, TestLocalSymbolRoundTrip, TestNamerPure per the feature behavior block
  - The RED commit also carries type-only scaffolding so the package compiles: scheme.go declares the full DeclKind value set (constants only — no ScipKind mapping, no package doc yet) and namer.go gains only the LocalSymbol(sourceName string, ordinal int) string signature with an empty-string return — no behavioral logic beyond the 01-01 tracer implementation
  - `cd swift && GOWORK=off go test ./internal/symbol/...` FAILS at this point (RED — the suite compiles against the scaffolding and every new case fails behaviorally: the tracer namer covers only the module+type+method path; new kinds, overloads, escaping, locals, and error paths are unimplemented)
  - Commit exists: `test(01-02): add failing golden table + round-trip properties for Swift symbol scheme`
  </acceptance_criteria>
  <files>swift/internal/symbol/namer_test.go, swift/internal/symbol/roundtrip_test.go, swift/internal/symbol/scheme.go (type-only scaffolding), swift/internal/symbol/namer.go (LocalSymbol signature only)</files>
  <action>
  Write the tests first, following the bindings table-test shape exactly (PATTERNS.md item 6): anonymous type test struct, t.Run keyed on expected string, require + cmp.Diff on strings. Copy the rapid skeleton from position_test.go via PATTERNS.md item 7 and fill the generators from the RESEARCH Code Examples block (bias names toward "==" , "<->", "🚀", "π", "F⃗", "back``tick", "$0", "a-b", "_x"). Extend the existing tracer test file rather than replacing it — the tracer case (MyApp Shape area) remains one row of the table. For the retroactive cases use the input shapes in the behavior block (Module "App" = owner module of the extended type). The tests reference the full DeclKind value set and LocalSymbol, which the 01-01 tracer does not declare — so the RED commit includes type-only scaffolding to keep the package compilable: add the DeclKind constants (one per mapping-row family) to scheme.go with no methods or doc, and a LocalSymbol(sourceName string, ordinal int) string signature in namer.go whose body only returns the empty string. No behavioral implementation in this task beyond that scaffolding. Run the suite, confirm the package compiles and every new test fails for the right reason (wrong/missing symbol strings, unsanitized locals — behavioral failures, not undeclared-identifier compile errors), commit with the test(01-02) message.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/scip/swift && GOWORK=off go test ./internal/symbol/... ; test $? -ne 0 && grep -cF 'TestNamerRoundTrip' internal/symbol/roundtrip_test.go | grep -qx '1' && grep -cF 'scip-swift swiftpm App . Shape#spike().' internal/symbol/namer_test.go | grep -qx '1' && grep -cF 'local count_2' internal/symbol/namer_test.go | grep -qx '1'</automated>
  </verify>
  <done>Golden table (all mapping rows) and rapid property tests exist, fail against the tracer namer, and the RED commit is recorded. Failures are behavioral (unimplemented kinds), not compile/syntax errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — frozen spec (scheme.go) + full namer implementation</name>
  <read_first>
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-RESEARCH.md ("Namer API shape", "Symbol Scheme Mapping Table", "Frozen-spec extras", "Don't Hand-Roll" items 1-2, "Code Examples" Namer core)
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-PATTERNS.md items 4 (scheme.go — symbol.go:9-21 comment discipline, CAUTION lines), 5 Excerpts A-E (build-then-format, "." placeholder, escaping-is-free, anti-pattern, local form)
  - /Users/ddphuong/Projects/jarvis-ai/scip/bindings/go/scip/symbol.go lines 9-21 (godoc + CAUTION style)
  - /Users/ddphuong/Projects/jarvis-ai/scip/bindings/go/scip/symbol_formatter.go lines 65-159
  - /Users/ddphuong/Projects/jarvis-ai/scip/swift/internal/symbol/namer.go (tracer implementation to expand)
  - /Users/ddphuong/Projects/jarvis-ai/scip/bindings/go/scip/scip.pb.go (grep for the exact generated SymbolInformation Kind constant names before referencing them)
  </read_first>
  <acceptance_criteria>
  - swift/internal/symbol/scheme.go declares: consts Scheme = "scip-swift", ManagerSwiftPM = "swiftpm", ManagerSystem = "swift"; type DeclKind with one value per mapping-row family (Module, Struct, Class, Enum, Protocol, TypeAlias, Func, Method, Operator, Constructor, Destructor, Getter, Setter, Property, Constant, Variable, Subscript, EnumCase, ProtocolMethod, TypeParameter, Parameter, Macro); method ScipKind() returning the scip.SymbolInformation Kind constants per the must_haves mapping table; a CAUTION godoc line that the scheme must not start with "local" (scip.proto:159)
  - swift/internal/symbol/scheme.go's package doc comment states the determinism rule, the canonical #if policy, the dual-path identity rule, and the Term-family retroactive-collision limitation (flagged_assumptions wording)
  - swift/internal/symbol/namer.go implements Symbol over the full SymbolInput space per the feature implementation notes: descriptor suffixes chosen by DeclKind (Namespace only for the module's own symbol; Type for types; Term for property/let/var/case; Method with Disambiguator "+N" for func/method/init/deinit/subscript/operator/getter/setter families; TypeParameter/Parameter/Macro for rows 26/27/29), ManagerSystem + SwiftToolchainVersion for IsSystemModule, Version "." for SwiftPM, validate-before-return via scip.ParseSymbol with the "namer produced unparseable symbol %q: %w" wrap
  - swift/internal/symbol/namer.go implements LocalSymbol(sourceName string, ordinal int) string with simple-identifier sanitization (non-identifier runes collapse to "_", ordinal greater than 0 appends "_N") building Symbol{Scheme: "local", Descriptors: [{Name: id, Suffix: Descriptor_Local}]}
  - `cd swift && GOWORK=off go test ./...` exits 0 (all golden + property tests green)
  - From repo root `go test ./swift/...` exits 0
  - Commit exists: `feat(01-02): implement frozen Swift symbol scheme + shared namer`
  </acceptance_criteria>
  <files>swift/internal/symbol/scheme.go, swift/internal/symbol/namer.go</files>
  <action>
  Complete scheme.go first (the RED commit already carries its type-only DeclKind scaffolding — add the consts, the ScipKind mapping, and the normative doc; verify the generated Kind constant names in scip.pb.go by grep before writing the mapping), then expand namer.go from the tracer to the full space, replacing the empty-return LocalSymbol signature with the real implementation. Every symbol string must come out of scip.VerboseSymbolFormatter.FormatSymbol over a constructed *scip.Symbol — escaping, the "." version placeholder, and disambiguator rendering are the formatter's job (PATTERNS.md item 5). Use Descriptor_Namespace, never the deprecated Descriptor_Package alias (RESEARCH pitfalls). OverloadIndex greater than 0 sets the Method descriptor's Disambiguator field to "+" followed by the decimal index — only on Method-family descriptors (the formatter renders disambiguators solely for Descriptor_Method; symbol_formatter.go:100-126). Getter/setter are Method-family (rows 14/15); the escaped `area=` form comes from the formatter because "=" is outside the simple-identifier set — add no escaping code. Locals: sanitize then build the local-form Symbol and let the formatter render "local <id>" (PATTERNS.md item 5 Excerpt E). Keep namer.go a pure function library: no I/O, no USR parsing, no global mutable state; empty Module/Name return an error sentinel. Run the suite until green; commit with the feat(01-02) message.
  <reversibility rating="costly">The scheme + strings are the frozen contract every golden in Phases 2-6 pins against — changing them later invalidates stored indexes and snapshots.</reversibility>
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/scip/swift && GOWORK=off go test ./... && cd /Users/ddphuong/Projects/jarvis-ai/scip && go test ./swift/... && grep -cF 'ManagerSwiftPM = "swiftpm"' swift/internal/symbol/scheme.go | grep -qx '1' && grep -cF 'func LocalSymbol(sourceName string, ordinal int) string' swift/internal/symbol/namer.go | grep -qx '1'</automated>
  </verify>
  <done>All golden cases and rapid properties pass from both swift/ (GOWORK=off) and the repo root; scheme.go carries the frozen constants, full DeclKind→Kind mapping, and the three normative rules; the namer builds and validates every string through the bindings formatter.</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR — descriptor-building helpers, README scheme section, formatter discipline sweep</name>
  <read_first>
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-RESEARCH.md ("Frozen-spec extras" — the three rules the README section must restate; "Common Pitfalls" hand-concatenation row)
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-PATTERNS.md item 2 (README prettier rules) and Shared Patterns "godoc / comments"
  - /Users/ddphuong/Projects/jarvis-ai/scip/swift/README.md (created in 01-01 Task 3)
  - /Users/ddphuong/Projects/jarvis-ai/scip/swift/internal/symbol/namer.go and scheme.go (current state)
  </read_first>
  <acceptance_criteria>
  - namer.go routes all descriptor construction through a shared helper (e.g. appendDescriptors) with no duplicated suffix-switch logic; tests stay green after the refactor
  - swift/README.md contains a "Symbol scheme" section stating: the header form scip-swift swiftpm <Module> . / scip-swift swift <Module> <swift-version>, the extension-attribution rule (SYM-02), the determinism rule, the canonical #if policy, the dual-path identity rule, and the Term-family retroactive-collision limitation
  - `grep -R -n 'Sprintf' swift/internal/symbol/namer.go swift/internal/symbol/scheme.go` matches nothing on descriptor-construction lines (an allowed use is formatting the "+N" disambiguator or error strings only — verify and note in SUMMARY)
  - `nix develop --command prettier --check swift/README.md` passes; `nix develop --command gofmt -l swift` outputs nothing
  - Full re-run: `cd swift && GOWORK=off go test ./...` and repo-root `go test ./swift/...` both exit 0
  - Commit exists: `refactor(01-02): extract descriptor helpers + document frozen scheme` (or the equivalent feat/docs commit if code cleanup was a no-op — then only the README commit)
  </acceptance_criteria>
  <files>swift/internal/symbol/namer.go, swift/README.md</files>
  <action>
  Clean up namer.go: collapse any duplicated descriptor-building switch logic into one helper; keep the DeclKind switch in scheme.go/namer.go the single place suffixes are chosen. Add the README "Symbol scheme" section per the acceptance criteria, prettier-formatted (semi false, single quotes in shell examples, es5 trailing commas). Sweep for hand-built descriptor strings — the only string construction allowed in the namer is the "+N" disambiguator and error-message wrapping. Re-run both test entry points; commit the refactor/docs commit.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/scip && go test ./swift/... && (cd swift && GOWORK=off go test ./...) && grep -cF '## Symbol scheme' swift/README.md | grep -qx '1' && ! grep -R -q 'Descriptor_Package' swift/internal/symbol/</automated>
  </verify>
  <done>Namer internals are factored without behavior change (tests green before and after), the README documents the frozen scheme, no deprecated Descriptor_Package alias and no hand-concatenated descriptor strings remain in the namer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Indexer layers → namer | SymbolInput fields (Module, Name, ContainerPath) originate from parsed source / LSP responses — semi-trusted input shapes cross into symbol identity |
| namer → emitted index | Every string the namer returns lands in a published .scip index consumed by external tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | swift/internal/symbol/namer.go | medium | mitigate | Validate-before-return: every produced string passes scip.ParseSymbol; malformed input (empty Module/Name) errors instead of emitting; the formatter's escaping (not hand-rolled code) guarantees grammar conformance |
| T-01-05 | Repudiation | scheme.go frozen mapping | low | accept | Pure-function determinism (TestNamerPure) + golden table make every emitted symbol reproducible and auditable; no untrusted input involved |
| T-01-06 | Denial of Service | Unicode/local sanitization | low | mitigate | LocalSymbol sanitizes any source name (incl. emoji/CJK) to a simple identifier before it reaches the local-form grammar, which rejects non-simple ids at parse time |

Blocking threshold: high (workflow.security_block_on) — no high-severity threats in this plan. ASVS L1.
</threat_model>

<verification>
- `cd swift && GOWORK=off go test ./...` and repo-root `go test ./swift/...` green.
- Golden table covers every mapping row; retroactive + cross-file extension cases present (SYM-02).
- rapid properties pass (default iterations); optionally a local `go test -fuzz` equivalent is not applicable here (rapid covers it).
- README prettier-clean; gofmt clean.
</verification>

<success_criteria>
- ROADMAP success criteria 1 and 2 hold: every category has a frozen string with round-trip property proofs (emoji/multi-byte included); extension members — cross-file and retroactive — name under the extended type's path via the shared namer.
</success_criteria>

## Artifacts this phase produces (plan 01-02)

- Final exported API of `github.com/scip-code/scip/swift/internal/symbol`: consts `Scheme`, `ManagerSwiftPM`, `ManagerSystem`; type `DeclKind` (+ ~22 values) with method `ScipKind()`; types `Container`, `SymbolInput`; funcs `Symbol(SymbolInput) (string, error)`, `LocalSymbol(sourceName string, ordinal int) string`.
- New files: swift/internal/symbol/scheme.go, swift/internal/symbol/roundtrip_test.go; expanded: namer.go, namer_test.go, README.md (Symbol scheme section).
- No new binaries; no go.mod changes (rapid/testify were pinned by 01-01).

<output>
Create `.planning/phases/01-symbol-scheme-module-foundations/01-02-SUMMARY.md` when done (include RED/GREEN/REFACTOR sections and the commit list per the TDD gate)
</output>
