---
phase: 01-symbol-scheme-module-foundations
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - swift/go.mod
  - swift/go.sum
  - checks.nix
  - swift/spikes/lsp_probe/main.go
  - swift/spikes/fixture/Package.swift
  - swift/spikes/fixture/Sources/CapabilityFixture/Core.swift
  - swift/spikes/fixture/Sources/CapabilityFixture/Extensions.swift
  - swift/spikes/fixture/Sources/CapabilityFixture/Generics.swift
  - swift/spikes/fixture/Sources/CapabilityFixture/Unicode.swift
  - swift/spikes/perf/generate/main.go
  - swift/spikes/perf/.gitignore
  - swift/spikes/2026-08-sourcekit-lsp-findings.md
  - .planning/STATE.md
autonomous: true
requirements: [SYM-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A runnable SourceKit-LSP probe driver exists at swift/spikes/lsp_probe (sourcegraph/jsonrpc2 v0.2.2 over stdio) that discovers the server via `xcrun -f sourcekit-lsp` (PATH fallback), records toolchain versions, performs the initialize/initialized handshake with the fixture as rootUri, gates readiness on the sourcekit/isIndexing extension request plus a known-symbol symbolInfo probe, and writes JSONL evidence (one record per request/response with latency ms)"
    - "Capability inventory with observed evidence exists for every hard case: overload USR distinctness, cross-file same-module extension attribution to the extended type, retroactive extension containerName, generics (generic func + generic type + generic method), protocol requirement + witness (including a call through the protocol existential), operator declarations, computed-var get/set accessors, Unicode declarations (func 🚀(), var π) and emoji string literals, and class inheritance + override — each recorded as yes/no with a response excerpt, never inferred from a cold/unready index"
    - "Harvest performance is measured on a ~500-file generated SwiftPM fixture and recorded: cold and warm time-to-ready, total harvest wall time, request count, requests/sec, p50/p95 request latency, server restarts/crashes, symbols + occurrences harvested, and server RSS at end — with the driver implementing the prescribed harvest discipline (one documentSymbol pass per file + one references pass per unique definition, bounded concurrency) so numbers are predictive"
    - "swift/spikes/2026-08-sourcekit-lsp-findings.md exists with: a metadata header, the capability inventory table, the measured-numbers tables, an explicit GO or NO-GO verdict evaluated against the recorded thresholds (symbolInfo distinct USRs for at least 95% of fixture defs incl. every overload; cross-file + retroactive extension attribution correct; perf-fixture harvest at most 30 min cold / 10 min warm with no unrecoverable server crash; call/type-hierarchy requests returning usable data), the index-store escape-hatch plan in the NO-GO case, and a how-to-re-run section (macOS + Xcode required, runtime-only)"
    - "The verdict is recorded as a decision in .planning/STATE.md (Accumulated Context → Decisions) referencing the findings doc"
    - "Adding jsonrpc2 to swift/go.mod does not break the Nix gate: the checks.nix swift attribute's vendorHash is refreshed via the paste-and-rerun flow (nix build .#checks.x86_64-linux.swift prints got: sha256-…; paste it; re-run until green) in the same task as the go.mod change, and `nix flake check` exits 0 at plan end (ROADMAP success criterion 4)"
  prohibitions:
    - statement: "Spike MUST NOT become a product: no code under swift/spikes/ is promoted into internal/, wired into the CLI, or referenced by non-spike packages — the Phase-3 LSP client is built fresh against the findings"
      status: unverified
      flagged: true
    - statement: "Spike evidence MUST NOT record capability conclusions from a cold or unready index — every harvest is preceded by the readiness gate (sourcekit/isIndexing false AND a known-symbol symbolInfo probe returning a non-empty USR), and cold vs warm runs are recorded separately, so an empty result is never written down as 'LSP cannot do X'"
      status: unverified
      flagged: true
  artifacts:
    - swift/spikes/lsp_probe/main.go
    - swift/spikes/fixture/ (Package.swift + Sources/CapabilityFixture/*.swift)
    - swift/spikes/perf/generate/main.go
    - swift/spikes/perf/.gitignore
    - swift/spikes/2026-08-sourcekit-lsp-findings.md
    - Evidence JSONL files under swift/spikes/ (capability + perf runs)
  key_links:
    - "lsp_probe driver -> sourcekit-lsp subprocess over stdio JSON-RPC (sourcegraph/jsonrpc2 v0.2.2)"
    - "findings doc -> Phase 3 planning (03-01/03-02 consume the capability inventory and verdict)"
    - "STATE.md decision entry -> findings doc path"
  flagged_assumptions:
    - "ASSUMED (RESEARCH gap 1): the go/no-go thresholds (>=95% USR coverage incl. every overload; <=30 min cold / <=10 min warm for ~500 files; no unrecoverable crash; usable hierarchy data) are proposed numbers with no measured baseline — the findings doc must state them as the agreed thresholds up front and record the measured values against them; if the user revises them, the verdict is re-evaluated before Phase 3 planning."
---

<objective>
Produce measured evidence and an explicit go/no-go verdict on the SourceKit-LSP semantic path for v1: a capability-inventory spike over a hard-case SwiftPM fixture (overloads, retroactive extensions, generics, witnesses, operators, accessors, Unicode) and a harvest-performance spike over a ~500-file generated fixture.

Purpose: Phase 3's architecture (LSP-client harvest) is only justified by evidence; this spike either validates it or triggers the documented index-store escape hatch while the symbol/emission layers survive unchanged.

Output: swift/spikes/lsp_probe driver, capability + perf fixtures, JSONL evidence with latencies, findings doc with capability inventory + numbers + verdict, STATE.md decision entry.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-symbol-scheme-module-foundations/01-RESEARCH.md
@.planning/phases/01-symbol-scheme-module-foundations/01-PATTERNS.md
@.planning/phases/01-symbol-scheme-module-foundations/01-01-SUMMARY.md
@swift/go.mod
@cmd/scip/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: SourceKit-LSP probe driver + capability fixture</name>
  <precondition>macOS host with Xcode toolchain: `xcrun -f sourcekit-lsp` exits 0 and `sourcekit-lsp --version` reports >= 6.1 (this repo's host has Xcode 26.3 / Swift 6.2.4 per RESEARCH). Halt and surface if absent — the spike cannot run without it.</precondition>
  <read_first>
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-RESEARCH.md (section "SourceKit-LSP Spike Protocol" — driver sequence steps 1-6, extension request shapes with cited LSP Extensions doc, fixture contents list, "Don't Hand-Roll" items 5 and 8)
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-PATTERNS.md item 3 (minimal main skeleton), item 14 (spike artifact rules: throwaway, no internal/ promotion, committed fixture data, git-ignored .build)
  - /Users/ddphuong/Projects/jarvis-ai/scip/cmd/scip/main.go lines 15-20 (main skeleton shape)
  - /Users/ddphuong/Projects/jarvis-ai/scip/swift/go.mod (add the jsonrpc2 require here)
  - /Users/ddphuong/Projects/jarvis-ai/scip/checks.nix (the swift buildGoModule attribute created by 01-01 Task 3 — its vendorHash goes stale when jsonrpc2 enters go.mod; refresh it here)
  </read_first>
  <acceptance_criteria>
  - swift/go.mod requires `github.com/sourcegraph/jsonrpc2 v0.2.2`; go.sum regenerated via `cd swift && GOWORK=off go mod tidy`
  - checks.nix swift attribute's vendorHash is refreshed for the jsonrpc2 dependency in the same task as the go.mod/go.sum change: first `nix build .#checks.x86_64-linux.swift` fails printing `got: sha256-…`; paste that hash over the old vendorHash and re-run until the build is green (expected buildGoModule flow — RESEARCH Common Pitfalls "Nix vendorHash churn on first build"; the 01-01 vendorHash only covered the pre-jsonrpc2 vendor tree)
  - swift/spikes/lsp_probe/main.go is package main using sourcegraph/jsonrpc2 over the server's stdio pipes; it takes flags -fixture (dir), -out (JSONL path), and -mode (capability); it discovers the server via `exec.Command("xcrun", "-f", "sourcekit-lsp")` with a PATH fallback, never a hardcoded absolute path, and records sourcekit-lsp/swift/xcodebuild versions into the first evidence record
  - The driver performs: initialize (rootUri = fixture, client capabilities incl. window.workDoneProgress; full InitializeResult.capabilities recorded) → initialized → readiness gate (poll sourcekit/isIndexing until false or timeout, then textDocument/symbolInfo at a known definition must return a non-empty USR before any harvest) → per file didOpen (text from disk, version 1) → textDocument/documentSymbol → per def range: textDocument/symbolInfo (name, containerName, usr, kind, isDynamic, receiverUsrs, systemModule.moduleName) + textDocument/definition + textDocument/references (includeDeclaration true) → for one call site and one type: prepareCallHierarchy + incomingCalls/outgoingCalls and prepareTypeHierarchy + supertypes/subtypes → shutdown/exit
  - swift/spikes/fixture/ is a committed SwiftPM library package (Package.swift + Sources/CapabilityFixture/{Core,Extensions,Generics,Unicode}.swift) containing every hard case from the RESEARCH fixture list: three overloads of f, a cross-file same-module extension of a type declared in Core.swift, a retroactive extension String, func id<T>/struct Box<T>/generic method, protocol Drawable + struct Circle witness + a call through the existential, static operator +, a computed var with get/set, func 🚀() and var π plus an emoji string literal, and a class inheritance + override pair
  - Running `go run ./spikes/lsp_probe -fixture ../fixture -out evidence-capability.jsonl -mode capability` from swift/spikes exits 0 and produces a JSONL file whose record count is at least (files + unique defs + hierarchy requests) with a latency_ms field on every record
  - `cd swift && GOWORK=off go build ./spikes/lsp_probe/ && go vet ./spikes/...` exits 0
  </acceptance_criteria>
  <files>swift/go.mod, swift/go.sum, checks.nix, swift/spikes/lsp_probe/main.go, swift/spikes/fixture/Package.swift, swift/spikes/fixture/Sources/CapabilityFixture/Core.swift, swift/spikes/fixture/Sources/CapabilityFixture/Extensions.swift, swift/spikes/fixture/Sources/CapabilityFixture/Generics.swift, swift/spikes/fixture/Sources/CapabilityFixture/Unicode.swift</files>
  <action>
  Add the jsonrpc2 dependency (`cd swift && GOWORK=off go mod tidy` regenerates go.sum), then immediately refresh the Nix vendorHash so ROADMAP success criterion 4 survives this plan: run `nix build .#checks.x86_64-linux.swift` from the repo root — it fails with a hash mismatch printing `got: sha256-…`; paste that hash into the checks.nix swift attribute's vendorHash and re-run until the build is green (expected paste-and-rerun buildGoModule flow; the new module dependency changes the vendored tree, so the 01-01 vendorHash is stale). Land the go.mod + go.sum + checks.nix changes in one commit. Then write the driver per the RESEARCH Spike Protocol sequence — that section, not an in-repo pattern, is the authority for the RPC loop (there is no in-repo jsonrpc2 precedent). Spawn the server with fixed argv (no shell interpolation of any flag or path), wire jsonrpc2 over the process's stdin/stdout, and log every request/response as one JSONL line with the request method, params, result summary, and latency. Record the full InitializeResult.capabilities verbatim — the inventory needs the advertised callHierarchyProvider/typeHierarchyProvider/positionEncoding answers. Implement the readiness gate exactly: poll sourcekit/isIndexing (empty params, result indexing bool) until false or a timeout, then probe symbolInfo at a known definition (e.g. the Shape struct decl in Core.swift) and require a non-empty USR before harvesting; record time-to-ready. Harvest per file with the prescribed discipline (documentSymbol once per file; symbolInfo/definition/references per unique def; hierarchy requests for exactly one call site and one type). Create the fixture by hand as committed Swift sources matching the acceptance list (swift package init --type library shape, library name CapabilityFixture, swift-tools-version aligned with the local toolchain). Keep everything throwaway-grade: package main under spikes/, no internal/ promotion, no CLI wiring.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/scip/swift && GOWORK=off go mod tidy && go build ./spikes/lsp_probe/ && go vet ./spikes/... && grep -cF 'jsonrpc2 v0.2.2' go.mod | grep -qx '1' && test -f spikes/fixture/Package.swift && grep -qF 'func' spikes/fixture/Sources/CapabilityFixture/Core.swift && grep -qF 'sourcekit/isIndexing' spikes/lsp_probe/main.go && cd /Users/ddphuong/Projects/jarvis-ai/scip && nix build .#checks.x86_64-linux.swift</automated>
  </verify>
  <done>The driver builds, runs end-to-end against the fixture on this host, the readiness gate precedes every harvest, and evidence-capability.jsonl exists with per-request latencies and the recorded InitializeResult.capabilities.</done>
</task>

<task type="auto">
  <name>Task 2: Perf fixture generator + harvest performance run</name>
  <precondition>Same toolchain precondition as Task 1; additionally expect the cold run to take tens of minutes — run it once, then the warm run, and record both (RESEARCH go/no-go thresholds).</precondition>
  <read_first>
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-RESEARCH.md (section "Performance spike" — fixture shape ~500 files x ~15 defs x ~10 refs, measured metrics list, harvest-discipline requirement)
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-PATTERNS.md item 14 (committed generator + Package.swift; .build/ git-ignored via a .gitignore inside swift/spikes/perf/)
  - /Users/ddphuong/Projects/jarvis-ai/scip/swift/spikes/lsp_probe/main.go (Task 1 driver — extend with -mode perf)
  </read_first>
  <acceptance_criteria>
  - swift/spikes/perf/generate/main.go is a committed Go generator that emits a SwiftPM package of at least 500 Swift files with roughly 15 definitions and 10 references each into a target dir (defaulting under swift/spikes/perf/gen/), deterministic given a fixed seed
  - swift/spikes/perf/.gitignore ignores the generated tree and any .build/ directory; the generator source and a Package.swift template are committed, generated output is not
  - The driver's -mode perf harvests the generated fixture with the prescribed discipline (one documentSymbol pass per file + one references pass per unique definition, bounded concurrency) and the run produces evidence-perf-cold.jsonl and evidence-perf-warm.jsonl
  - The evidence records (or a small -summary output) contain: cold time-to-ready, warm time-to-ready, total harvest wall time, request count, requests/sec, p50 and p95 request latency, server restarts/crashes count, symbols + occurrences harvested, and server RSS at end
  - `cd swift && GOWORK=off go build ./spikes/perf/generate/ && go vet ./spikes/...` exits 0
  </acceptance_criteria>
  <files>swift/spikes/perf/generate/main.go, swift/spikes/perf/.gitignore, swift/spikes/lsp_probe/main.go</files>
  <action>
  Write the generator (deterministic content: structs/classes/protocols/methods/properties with cross-file references, seeded so re-generation is byte-stable), generate the tree, and add the perf mode to the driver: measure time-to-ready cold (fresh server on the untouched tree — optionally delete .build/ first) and warm (second run), then the full harvest with bounded concurrency (a small worker pool, e.g. 4-8 concurrent requests — record the bound used). Collect p50/p95 from the per-request latencies already in the JSONL; capture server RSS via the process's rusage/os state at end; count any restart/crash. Commit generator + .gitignore + Package.swift template only; keep generated trees and .build/ ignored. Record the numbers into the findings doc in Task 3.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/scip/swift && GOWORK=off go build ./spikes/perf/generate/ && go vet ./spikes/... && test -f spikes/perf/.gitignore && grep -qF 'mode' spikes/lsp_probe/main.go</automated>
  </verify>
  <done>Cold and warm perf runs completed with evidence files; all nine prescribed metrics recorded; generated output not committed.</done>
</task>

<task type="auto">
  <name>Task 3: Findings doc — capability inventory, measured numbers, go/no-go verdict, STATE.md decision</name>
  <read_first>
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-RESEARCH.md (section "Go/no-go criteria" — the five GO conditions and the NO-GO index-store escape hatch; section "Confidence Gaps" item 1 on threshold provenance)
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/research/01-*.md doc style if present, else the structure of 01-RESEARCH.md itself (title + metadata header, tables, explicit verdict, how-to-re-run)
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/STATE.md (Accumulated Context → Decisions section where the verdict lands)
  - swift/spikes/evidence-capability.jsonl, evidence-perf-cold.jsonl, evidence-perf-warm.jsonl (the measured inputs)
  </read_first>
  <acceptance_criteria>
  - swift/spikes/2026-08-sourcekit-lsp-findings.md contains: a metadata header (date, host, toolchain versions captured from evidence), a "Thresholds" section stating the agreed go/no-go thresholds verbatim from must_haves (and flagging them as assumed numbers per the flagged assumption), a "Capability inventory" table with one row per hard case (overloads/USR distinctness, cross-file extension attribution, retroactive containerName, generics, protocol witness + existential call, operator, accessors, Unicode, override, call/type-hierarchy providers) each with observed yes/no + response excerpt, a "Performance" section with the cold/warm numbers tables, a "Verdict" section with an explicit single word GO or NO-GO plus the per-threshold evaluation, and a "How to re-run" section (commands for generator, driver cold/warm, toolchain requirements, runtime-only note)
  - In the NO-GO case the findings doc includes the index-store escape-hatch plan paragraph (bulk harvest swaps to reading .build/index/store via a Swift helper subprocess or cgo libIndexStore; LSP kept for live queries; symbol/emission layers unchanged)
  - .planning/STATE.md Decisions section gains an entry dated 2026-08-16+ recording the verdict with a reference to the findings doc path
  - `nix develop --command prettier --check swift/spikes/2026-08-sourcekit-lsp-findings.md` passes
  - The findings doc filename string "2026-08-sourcekit-lsp-findings.md" appears in STATE.md's new decision entry
  </acceptance_criteria>
  <files>swift/spikes/2026-08-sourcekit-lsp-findings.md, .planning/STATE.md</files>
  <action>
  Write the findings doc strictly from the recorded evidence — every inventory row cites a response excerpt from the JSONL, every number comes from the perf runs; when a capability was not exercised or the probe errored, write that explicitly rather than inferring. Evaluate the verdict mechanically against the stated thresholds, one bullet per threshold with pass/fail. If any threshold fails, the verdict is NO-GO and the escape-hatch paragraph is mandatory. Add the STATE.md decision entry (one line, e.g.: "[Phase 1 spike]: SourceKit-LSP semantic path GO/NO-GO — see swift/spikes/2026-08-sourcekit-lsp-findings.md"). Keep the doc prettier-clean. Note: the verdict changes Phase 3's shape but is itself reversible evidence — recording NO-GO does not delete any code.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/scip && grep -cF '## Verdict' swift/spikes/2026-08-sourcekit-lsp-findings.md | grep -qx '1' && grep -cF 'How to re-run' swift/spikes/2026-08-sourcekit-lsp-findings.md | grep -qx '1' && grep -cF '2026-08-sourcekit-lsp-findings.md' .planning/STATE.md | grep -qx '1' && grep -cE 'GO|NO-GO' swift/spikes/2026-08-sourcekit-lsp-findings.md >/dev/null && echo verdict-present</automated>
  </verify>
  <done>Findings doc complete with inventory, numbers, explicit verdict evaluated per threshold, escape-hatch plan when NO-GO, re-run instructions; STATE.md records the decision; evidence is reproducible from the documented commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Probe driver → sourcekit-lsp subprocess | The driver spawns a discovered binary and exchanges JSON-RPC over stdio; server responses are semi-trusted input parsed into evidence records |
| Host environment → server discovery | `xcrun -f sourcekit-lsp` / PATH resolution determines which binary runs — a hijacked PATH would substitute the server |
| Fixture files → server | In-repo committed Swift sources (trusted, reviewed); no untrusted source flows into the spike |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Elevation of Privilege / Tampering | lsp_probe server discovery | low | mitigate | Discovery only via `exec.Command("xcrun", "-f", "sourcekit-lsp")` with fixed argv (no shell, no string interpolation of flags/paths); toolchain versions recorded into evidence so a substituted binary is visible in the record |
| T-01-08 | Denial of Service | driver ↔ server loop | low | mitigate | Readiness-gate polling has a timeout; bounded request concurrency; server crash counts recorded — the spike degrades to recorded evidence, never to a hung build |
| T-01-09 | Information Disclosure | evidence JSONL | low | accept | Evidence contains only in-repo fixture symbols and latencies; written under swift/spikes/, not committed as secrets-bearing |
| T-01-SC | Tampering | package installs | n/a | accept | Single new Go dependency github.com/sourcegraph/jsonrpc2 v0.2.2 — the project-decided LSP client lib per .planning STACK.md (project research), outside the npm/pip/cargo package-legitimacy gate's scope; no other new packages |

Blocking threshold: high (workflow.security_block_on) — no high-severity threats in this plan. ASVS L1.
</threat_model>

<verification>
- `cd swift && GOWORK=off go build ./spikes/... && go vet ./spikes/...` green.
- `nix build .#checks.x86_64-linux.swift` green with the refreshed vendorHash (jsonrpc2 now vendored), and `nix flake check` exits 0 — ROADMAP success criterion 4 must still hold after this plan's go.mod change (slow; run once at plan end).
- Capability evidence JSONL exists with readiness-gate records preceding harvest records.
- Perf evidence (cold + warm) exists with all nine metrics.
- Findings doc passes the section grep gates and prettier; STATE.md carries the verdict decision.
</verification>

<success_criteria>
- ROADMAP success criterion 5 holds: spike findings recorded with measured evidence — capability inventory (overloads, retroactive extensions, generics, witnesses) and harvest performance on a fixture repo — plus an explicit go/no-go verdict on the LSP semantic path for v1.
</success_criteria>

## API Coverage — external surface exercised by this phase (decision)

The spike is the coverage instrument for the SourceKit-LSP request surface; the production LSP client is Phase 3 (03-01). Enumerated surface, each exercised at least once by the driver: initialize, initialized, shutdown, exit, textDocument/didOpen, textDocument/documentSymbol, textDocument/definition, textDocument/references, textDocument/symbolInfo (extension), sourcekit/isIndexing (extension), sourcekit/workspace/symbolNames (extension, optional probe), sourcekit/workspace/triggerReindex (extension, recovery only), callHierarchy/prepare, callHierarchy/incomingCalls, callHierarchy/outgoingCalls, typeHierarchy/prepare, typeHierarchy/supertypes, typeHierarchy/subtypes. Decision: covered-by-evidence in Phase 1 (the inventory records which of these the server actually supports, with response excerpts); production implementation coverage is deferred to Phase 3 planning by design, not silently dropped.

## Artifacts this phase produces (plan 01-03)

- New package: `github.com/scip-code/scip/swift/spikes/lsp_probe` (package main; run via `go run`, no installed binary).
- New package: `github.com/scip-code/scip/swift/spikes/perf/generate` (package main generator).
- New committed data: swift/spikes/fixture/ (SwiftPM package CapabilityFixture), swift/spikes/perf/.gitignore.
- New doc: swift/spikes/2026-08-sourcekit-lsp-findings.md (evidence + verdict).
- Evidence outputs (generated at runtime, JSONL): swift/spikes/evidence-capability.jsonl, evidence-perf-cold.jsonl, evidence-perf-warm.jsonl.
- swift/go.mod gains github.com/sourcegraph/jsonrpc2 v0.2.2 (with go.sum regeneration); checks.nix swift vendorHash refreshed for the new dependency (paste-and-rerun).
- .planning/STATE.md gains the Phase-1 spike verdict decision entry.

<output>
Create `.planning/phases/01-symbol-scheme-module-foundations/01-03-SUMMARY.md` when done
</output>
