---
phase: 01-symbol-scheme-module-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - swift/go.mod
  - swift/go.sum
  - swift/cmd/scip-swift/main.go
  - swift/internal/symbol/namer.go
  - swift/internal/symbol/namer_test.go
  - go.work
  - go.work.sum
  - bindings/go/scip/symbol_parser.go
  - bindings/go/scip/symbol_test.go
  - bindings/go/scip/symbol_fuzz_test.go
  - .github/workflows/ci.yaml
  - checks.nix
  - swift/README.md
autonomous: true
requirements: [SYM-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tracer: the swift/ Go module builds inside the go.work workspace and a passing test in swift/internal/symbol emits one real Swift symbol string (scip-swift swiftpm MyApp . Shape#area().) through scip.VerboseSymbolFormatter and round-trips it through scip.ParseSymbol + FormatSymbol as the identity — proving module wiring + symbol identity pipeline end-to-end on day one (walking-skeleton slice, see SKELETON.md)"
    - "scip.ParseSymbol returns an error for the three trailing-multi-byte-rune inputs (a b c d fooΩ / test . pkg . barΩ / a b c d `foo`Ω) instead of a runtime panic (ROADMAP success criterion 3; root cause: peekNext guard tests byteIndex+1 but reads byteIndex+bytesToNextRune)"
    - "FuzzParseSymbol exists in bindings/go/scip as a native testing.F target whose seed corpus runs green under plain go test AND under -tags asserts, asserting ParseSymbol error-or-success and, on success, VerboseSymbolFormatter.Format(parsed) == input"
    - "All pre-existing bindings/go/scip tests stay green after the fix, including -tags asserts and the ./memtest low-memory suite (ROADMAP success criterion 3)"
    - "swift/go.mod declares module github.com/scip-code/scip/swift with a relative replace to ../bindings/go/scip, go 1.25.0, and requires scip bindings + testify v1.11.1 + pgregory.net/rapid v1.3.0; go.work use block lists swift; go test passes from repo root (workspace mode) AND from swift/ with GOWORK=off"
    - "Repo wiring: ci.yaml paths-filter lists swift/go.mod and swift/go.sum, the tidy loop iterates over swift, and the nix-update attr list includes checks.x86_64-linux.swift; checks.nix defines a swift buildGoModule attribute (modRoot ./swift, env.GOWORK off) that nix flake check builds green (ROADMAP success criterion 4)"
  prohibitions:
    - statement: "MUST NOT trade the ParseSymbol panic for silent wrong results — an invalid symbol string must surface as a returned error, never as a partially-parsed *scip.Symbol (the project's never-silently-wrong contract)"
      status: unverified
      flagged: true
  artifacts:
    - swift/go.mod
    - swift/go.sum
    - swift/cmd/scip-swift/main.go
    - swift/internal/symbol/namer.go
    - swift/internal/symbol/namer_test.go
    - swift/README.md
    - bindings/go/scip/symbol_fuzz_test.go
  key_links:
    - "swift/internal/symbol/namer.go -> bindings/go/scip (scip.Symbol, scip.VerboseSymbolFormatter, scip.ParseSymbol) via the relative replace directive"
    - "go.work use block -> swift/ module (workspace builds)"
    - "checks.nix swift attribute -> flake.nix checks (auto-discovered by the CI list/checks matrix)"
    - "ci.yaml paths-filter + tidy loop + nix-update attr -> swift/ module manifests (same-commit wiring per RESEARCH Pitfall 13)"
  flagged_assumptions: []
---

<objective>
Stand up the new `swift/` Go module (github.com/scip-code/scip/swift) wired into go.work / CI / Nix, fix the `scip.ParseSymbol` multi-byte-trailing-rune panic with a regression + fuzz test, and prove the module + symbol-identity pipeline end-to-end with a tracer slice that round-trips one real Swift symbol string.

Purpose: This is the walking-skeleton plan for Phase 1 (see SKELETON.md). It removes the pre-existing parser DoS/panic blocker (STATE.md Blockers) before any mass symbol emission, and it creates the module home that plans 01-02 (namer) and 01-03 (spikes) build inside.

Output: A building, workspace-wired, CI/Nix-visible `swift/` module with a tracer namer test; a fixed `peekNext` guard; a `FuzzParseSymbol` fuzz target; ci.yaml/checks.nix wiring.

Scope note: files_modified lists 13 entries — above the 10-file warning threshold — but they distribute cleanly across the three tasks (7 module+tracer manifests in Task 1, 3 parser-fix files in Task 2, 3 wiring files in Task 3), several are generated or one-line (go.sum, go.work, go.work.sum), and the calibrated estimate stays ~40k tokens (0.40 ratio); acknowledged, no split required.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-symbol-scheme-module-foundations/01-RESEARCH.md
@.planning/phases/01-symbol-scheme-module-foundations/01-PATTERNS.md
@go.work
@reprolang/go.mod
@bindings/go/scip/symbol_parser.go
@bindings/go/scip/symbol_formatter.go
@bindings/go/scip/symbol_test.go
@checks.nix
@.github/workflows/ci.yaml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end symbol-identity slice — swift/ module + one round-tripped Swift symbol</name>
  <read_first>
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-RESEARCH.md (sections "Module layout (plan 01-01)", "Namer API shape (plan 01-02)", "Module Wiring Checklist" items 1-2, 6-7, "Code Examples" go.work entry)
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-PATTERNS.md items 1 (swift/go.mod analog, reprolang/go.mod:1-12 excerpt), 3 (cmd stub, cmd/scip/main.go:15-20), 5 Excerpts A/B/C/E (build-then-format path, "." version placeholder, free escaping, local form), 7 (roundtrip_test generator pattern), 11 (go.work append)
  - /Users/ddphuong/Projects/jarvis-ai/scip/reprolang/go.mod (verbatim module-manifest shape to copy)
  - /Users/ddphuong/Projects/jarvis-ai/scip/go.work (current 3-module workspace)
  - /Users/ddphuong/Projects/jarvis-ai/scip/bindings/go/scip/symbol_formatter.go lines 65-91 (FormatSymbol), 131-159 (writeEscapedPackage, writeSuffixedDescriptor)
  - /Users/ddphuong/Projects/jarvis-ai/scip/bindings/go/scip/symbol_test.go lines 10-28 and 125-134 (table-test shape)
  </read_first>
  <acceptance_criteria>
  - swift/go.mod contains the exact strings: `module github.com/scip-code/scip/swift`, `go 1.25.0`, `replace github.com/scip-code/scip/bindings/go/scip => ../bindings/go/scip`, `github.com/scip-code/scip/bindings/go/scip v0.0.0-00010101000000-000000000000`, `github.com/stretchr/testify v1.11.1`, `pgregory.net/rapid v1.3.0`
  - swift/go.sum exists (generated by `cd swift && GOWORK=off go mod tidy`, never hand-written)
  - go.work use block contains `swift` (produced by `go work use ./swift` from repo root); go.work.sum committed together with go.work
  - swift/internal/symbol/namer.go declares `const Scheme = "scip-swift"` and `const ManagerSwiftPM = "swiftpm"`, types `SymbolInput` (fields Module, IsSystemModule, SwiftToolchainVersion, ContainerPath, Name, Kind, OverloadIndex), `Container` (fields Name, Kind), a minimal `DeclKind` set for this slice (DeclKindStruct, DeclKindMethod), and `func Symbol(in SymbolInput) (string, error)` that builds a `*scip.Symbol` (Package Manager ManagerSwiftPM, Name in.Module, Version ".") with Type then Method descriptors and formats via `scip.VerboseSymbolFormatter.FormatSymbol`, then validates the result with `scip.ParseSymbol` before returning it
  - swift/internal/symbol/namer_test.go contains a test asserting Symbol(SymbolInput{Module: "MyApp", ContainerPath: []Container{{Name: "Shape", Kind: DeclKindStruct}}, Name: "area", Kind: DeclKindMethod, OverloadIndex: 0}) == "scip-swift swiftpm MyApp . Shape#area()." and that ParseSymbol + VerboseSymbolFormatter.FormatSymbol return the identical string
  - swift/cmd/scip-swift/main.go is a minimal `package main` + `func main() {}` stub (full CLI surface is Phase 2+, per PATTERNS.md item 3)
  - From repo root `go build ./... && go test ./swift/...` exits 0; from swift/ `GOWORK=off go test ./...` exits 0
  </acceptance_criteria>
  <files>swift/go.mod, swift/go.sum, swift/cmd/scip-swift/main.go, swift/internal/symbol/namer.go, swift/internal/symbol/namer_test.go, go.work, go.work.sum</files>
  <behavior>
  - Test 1 (RED first — write before namer.go exists; the fresh module's compile failure is the red state): Symbol of a method area on struct Shape in module MyApp produces exactly "scip-swift swiftpm MyApp . Shape#area()."
  - Test 2: scip.ParseSymbol("scip-swift swiftpm MyApp . Shape#area().") returns no error, parsed.Scheme == "scip-swift", and scip.VerboseSymbolFormatter.FormatSymbol(parsed) == the input string (identity)
  - Test 3: go.work workspace mode resolves package github.com/scip-code/scip/swift/internal/symbol and its test runs from the repo root
  </behavior>
  <action>
  RED: create the module manifest and the test first. Write swift/go.mod copying the reprolang/go.mod shape (PATTERNS.md item 1): module github.com/scip-code/scip/swift, go 1.25.0, relative replace to ../bindings/go/scip, require block with the scip bindings placeholder version, testify v1.11.1, and pgregory.net/rapid v1.3.0 (rapid is required now so plan 01-02 adds no go.mod churn). Run `cd swift && GOWORK=off go mod tidy` to generate go.sum. Write swift/internal/symbol/namer_test.go with the round-trip test above (golden + identity assertions, require style). Run the test — it fails to compile (no namer.go): that is the RED state.
  GREEN: implement swift/internal/symbol/namer.go as the tracer-slice namer. It must construct protobuf `*scip.Symbol` values and format them with `scip.VerboseSymbolFormatter.FormatSymbol` — descriptor strings come only from the bindings formatter, which auto-backtick-escapes non-simple identifiers and maps empty version to "." (PATTERNS.md item 5 Excerpts B/C). Do NOT format symbols with fmt.Sprintf or string concatenation of descriptor suffixes (the reprolang newGlobalSymbol Sprintf pattern is the documented anti-pattern). Validate every produced string with scip.ParseSymbol and wrap failures as `fmt.Errorf("namer produced unparseable symbol %q: %w", s, err)`. Declare the full SymbolInput field set (including fields this slice does not read — IsSystemModule, SwiftToolchainVersion, OverloadIndex) so plan 01-02 expands behavior without changing the struct shape. Add swift/cmd/scip-swift/main.go stub. Run the test — green.
  WIRE: from repo root run `go work use ./swift`; commit go.work + go.work.sum together. Confirm `go build ./... && go test ./swift/...` from the root and `GOWORK=off go test ./...` inside swift/ both pass. The relative replace directive is mandatory — Nix buildGoModule and CI build per-module with GOWORK=off (RESEARCH gotcha (a)).
  <reversibility rating="costly">The module path github.com/scip-code/scip/swift is cheap to create but a later rename ripples through go.work, CI, Nix, and every import — get the path right now.</reversibility>
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/scip && go build ./... && go test ./swift/... && (cd swift && GOWORK=off go test ./...) && grep -F 'module github.com/scip-code/scip/swift' swift/go.mod && grep -cF 'swift' go.work | grep -qx '1' && test -s swift/go.sum</automated>
  </verify>
  <done>The swift/ module exists in the workspace, builds and tests green from both repo root (workspace mode) and swift/ (GOWORK=off), and the tracer test proves one real Swift symbol string round-trips through scip.ParseSymbol/VerboseSymbolFormatter as the identity. go.work and go.work.sum committed together.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ParseSymbol multi-byte panic fix — regression tests, one-line guard fix, fuzz target</name>
  <read_first>
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-RESEARCH.md (section "ParseSymbol Fix" — root cause, empirically verified inputs, minimal fix, tests to add, blast-radius guardrails, upstream status)
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-PATTERNS.md items 8 (bug site excerpt, symbol_parser.go:374-379), 9 (TestParseSymbolError harness excerpt, symbol_test.go:136-155), 10 (fuzz-target design replacing the legacy gofuzz import at parse_test.go:13)
  - /Users/ddphuong/Projects/jarvis-ai/scip/bindings/go/scip/symbol_parser.go lines 267-299 (findRuneAtIndex pre-conditions), 374-379 (peekNext), 418-419 and 529 (reach paths)
  - /Users/ddphuong/Projects/jarvis-ai/scip/bindings/go/scip/symbol_test.go lines 136-155 (existing TestParseSymbolError) and lines 15-124 (TestParseSymbol corpus, seed source)
  - /Users/ddphuong/Projects/jarvis-ai/scip/bindings/go/scip/parse_test.go line 13 (legacy `fuzz "github.com/google/gofuzz"` import this target supersedes — do not extend it)
  </read_first>
  <acceptance_criteria>
  - bindings/go/scip/symbol_test.go TestParseSymbolError list additionally contains the three inputs "a b c d fooΩ", "test . pkg . barΩ", "a b c d `foo`Ω" (verbatim, appended to the existing list; the existing require.NotPanics + error-non-nil harness asserts both properties)
  - bindings/go/scip/symbol_fuzz_test.go exists and declares `func FuzzParseSymbol(f *testing.F)` using only stdlib testing.F; seed corpus includes every string literal from TestParseSymbol and TestParseSymbolError plus the three multi-byte-tail inputs plus escaped-Unicode forms such as "scip swift MyMod . `🚀`."
  - The fuzz property asserts: ParseSymbol does not crash, and when it returns no error, scip.VerboseSymbolFormatter.Format(parsed) == input
  - symbol_fuzz_test.go additionally declares TestParseSymbolRuneSoupNeverPanics (RESEARCH "ParseSymbol Fix" tests-to-add item 3, dropped from no task until this revision): a pgregory.net/rapid rune-soup generator (rapid.String-derived, biased to multi-byte runes at the string tail — position_test.go rapid pattern per PATTERNS.md item 7) feeds ParseSymbol and asserts only never-panic; error-or-success both acceptable (rapid is already a bindings/go/scip test dep via position_test.go — no go.mod change)
  - bindings/go/scip/symbol_parser.go peekNext guard line is exactly `if z.byteIndex+int(z.bytesToNextRune) < len(z.SymbolString) {` (the read on the next line is untouched) and no other line of the file changed
  - `cd bindings/go/scip && go test ./...` exits 0 and `go test -tags asserts ./...` exits 0 (the -tags asserts run includes ./memtest)
  </acceptance_criteria>
  <files>bindings/go/scip/symbol_parser.go, bindings/go/scip/symbol_test.go, bindings/go/scip/symbol_fuzz_test.go</files>
  <behavior>
  - Test 1 (RED first): TestParseSymbolError with the three appended inputs fails before the fix — the harness reports a panic ("panic when parsing %q") for each trailing-multi-byte input
  - Test 2: after the fix, all three inputs return a non-nil error and no panic; the error is the existing unrecognizedDescriptorError path (symbol_parser.go:448-456 fall-through)
  - Test 3 (fuzz seed corpus): plain `go test -run FuzzParseSymbol ./...` passes — seeds run as unit tests; a valid escaped form like "scip swift MyMod . `🚀`." still parses and round-trips
  - Test 4 (non-regression): TestParseSymbol, TestSymbolFormatterRoundTrip, and the full module suite including ./memtest stay green under both plain and -tags asserts builds
  - Test 5 (rapid property): rune-soup inputs with multi-byte tails (rapid generator) never panic ParseSymbol — error-or-success both acceptable (RESEARCH tests-to-add item 3)
  </behavior>
  <action>
  RED: append the three panic-repro inputs to the TestParseSymbolError list in bindings/go/scip/symbol_test.go (PATTERNS.md item 9 excerpt shows the exact harness — change nothing else in it). Create bindings/go/scip/symbol_fuzz_test.go with FuzzParseSymbol per PATTERNS.md item 10: stdlib testing.F only, f.Add seeds sourced from the existing test corpora plus the multi-byte tails plus escaped `🚀` forms, property = no crash + success implies VerboseSymbolFormatter.Format(parsed) == input; in the same file add TestParseSymbolRuneSoupNeverPanics — a rapid rune-soup property (generator biased to multi-byte runes at the tail, position_test.go skeleton via PATTERNS.md item 7) asserting only no-panic (RESEARCH tests-to-add item 3 — the file's named home). Run `cd bindings/go/scip && go test -run 'TestParseSymbolError|FuzzParseSymbol|TestParseSymbolRuneSoupNeverPanics' ./...` — TestParseSymbolError must fail with the panic. Commit gate: `test(01-01): add failing regression + fuzz corpus for ParseSymbol multi-byte panic`.
  GREEN: apply the single-line guard change in bindings/go/scip/symbol_parser.go peekNext (line 375): the guard must test the byte offset that findRuneAtIndex will actually read, i.e. byteIndex plus bytesToNextRune, instead of byteIndex plus one (exact target line is in acceptance criteria; the bug-site verbatim and reach-path analysis are in PATTERNS.md item 8 and RESEARCH "ParseSymbol Fix"). Change nothing else: findRuneAtIndex pre-conditions, descriptorsWriter slot-reuse logic, and advanceOneByte semantics are protected by ./memtest and -tags asserts (RESEARCH blast-radius guardrails). Run the full suite: `go test ./... && go test -tags asserts ./...` in bindings/go/scip — all green. Commit gate: `feat(01-01): fix peekNext guard so trailing multi-byte runes return an error`.
  Local-only extra (never wired into CI): a short `go test -fuzz=FuzzParseSymbol -fuzztime=60s ./...` soak in bindings/go/scip to confirm the corpus explores; record the outcome in the SUMMARY.
  Note: the fix is upstream-worthy — record in the SUMMARY a reminder to open a scip-code/scip issue with the repro + one-line fix after the phase lands (RESEARCH "Upstream status"); do not block on it.
  <reversibility rating="reversible">A single guard-line change in a hand-written file, guarded by regression + fuzz + memtest suites.</reversibility>
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/scip/bindings/go/scip && go test ./... && go test -tags asserts ./... && grep -cF 'if z.byteIndex+int(z.bytesToNextRune) < len(z.SymbolString) {' symbol_parser.go | grep -qx '1' && grep -cF 'a b c d fooΩ' symbol_test.go | grep -qx '1' && grep -cF 'func FuzzParseSymbol(f *testing.F)' symbol_fuzz_test.go | grep -qx '1' && grep -qF 'TestParseSymbolRuneSoupNeverPanics' symbol_fuzz_test.go && grep -qF 'pgregory.net/rapid' symbol_fuzz_test.go</automated>
  </verify>
  <done>ParseSymbol returns errors (no panic) for symbol strings ending in multi-byte runes; FuzzParseSymbol seed corpus and the TestParseSymbolRuneSoupNeverPanics rapid property are green under plain go test and -tags asserts; every pre-existing bindings/go/scip test including ./memtest stays green; the diff in symbol_parser.go is exactly one guard line.</done>
</task>

<task type="auto">
  <name>Task 3: Repo wiring — ci.yaml paths-filter/tidy/nix-update, checks.nix swift attribute, module README</name>
  <read_first>
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-RESEARCH.md (section "Module Wiring Checklist" items 2-7 and the Gotchas paragraph)
  - /Users/ddphuong/Projects/jarvis-ai/scip/.planning/phases/01-symbol-scheme-module-foundations/01-PATTERNS.md items 2 (README prettier rules, .prettierrc:1-5), 12 (ci.yaml three excerpts at lines 34-43, 71-80, 82-97 and the list/checks-matrix auto-discovery at 132-157), 13 (checks.nix go-bindings excerpt at lines 47-61)
  - /Users/ddphuong/Projects/jarvis-ai/scip/.github/workflows/ci.yaml lines 34-43, 71-97, 132-157
  - /Users/ddphuong/Projects/jarvis-ai/scip/checks.nix lines 47-61 (go-bindings attribute to mirror)
  - /Users/ddphuong/Projects/jarvis-ai/scip/reprolang/README.md (module-README tone/structure precedent)
  </read_first>
  <acceptance_criteria>
  - .github/workflows/ci.yaml paths-filter relevant list contains `- 'swift/go.mod'` and `- 'swift/go.sum'`
  - .github/workflows/ci.yaml Tidy Go modules step loop line reads `for dir in bindings/go/scip . reprolang swift; do`
  - .github/workflows/ci.yaml nix-update attr list contains `checks.x86_64-linux.swift` as one attr invocation
  - checks.nix contains a `swift = pkgs.buildGoModule {` attribute with `pname = "scip-swift";`, `inherit version;`, `src = ./.;`, `modRoot = "./swift";`, a real `vendorHash = "sha256-…"` (pasted from the first nix build output, never a placeholder), `env.GOWORK = "off";`, `subPackages` listing "." and "internal/symbol", and `installPhase = "touch $out";` — and no version.txt assertion (verified inapplicable, RESEARCH constraint 7)
  - `nix eval .#checks.x86_64-linux --apply builtins.attrNames --json` output contains "swift"
  - `nix flake check` exits 0 (existing checks + formatting + the new swift check)
  - swift/README.md exists, states that sourcekit-lsp/Xcode is a runtime-only prerequisite (never a build dependency), includes a "Running tests" section, and passes `nix develop --command prettier --check swift/README.md`
  - flake.nix is unchanged (checks import picks the new attribute up automatically)
  </acceptance_criteria>
  <files>.github/workflows/ci.yaml, checks.nix, swift/README.md</files>
  <action>
  Edit .github/workflows/ci.yaml in the fix job, three changes per PATTERNS.md item 12 excerpts: (1) add swift/go.mod and swift/go.sum to the paths-filter relevant list; (2) add swift to the tidy loop's for-dir list (the step already sets GOWORK off); (3) add checks.x86_64-linux.swift to the nix-update for-attr list (one attribute per invocation, sequential). Do NOT add a new CI job — the list job enumerates flake checks dynamically and the checks matrix builds each (ci.yaml:132-157). Do NOT add swift paths to the release workflow (releases trigger on cmd/scip/version.txt only).
  Edit checks.nix: add the swift attribute mirroring the go-bindings entry (PATTERNS.md item 13 excerpt) with the values in the acceptance criteria; keep `buildTags = [ "asserts" ];` for house-style parity (optional for a module with no assert() calls, but consistent); format with nixfmt. First `nix build .#checks.x86_64-linux.swift` prints the correct vendorHash — paste it and re-run until green (expected buildGoModule flow, RESEARCH Gotcha (b)).
  Write swift/README.md in the reprolang-README tone: intro paragraph (what scip-swift is, why it exists — SCIP indexes for Swift powering jarvis navigation), short sections, fenced bash blocks for the sanity commands (repo-root go build ./... && go test ./...; swift/ GOWORK=off go test ./...; nix flake check), a "Runtime prerequisites" section stating Swift >= 6.1 / sourcekit-lsp discovered via `xcrun -f sourcekit-lsp` is runtime-only, and a "Running tests" section. Prettier rules apply (.prettierrc: semi false, singleQuote, es5 trailing commas) — run `nix develop --command prettier --check swift/README.md` before committing.
  All ci.yaml/checks.nix edits land in the SAME commit as swift/go.mod + swift/go.sum (already committed in Task 1; if the executor's branch splits commits, ensure the wiring commit also touches the manifests' history atomically — practically: this task's commit contains the ci.yaml + checks.nix + README changes and no other semantic drift, per RESEARCH Pitfall 13 / project research Pitfall 13).
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/scip && grep -cF "swift/go.mod" .github/workflows/ci.yaml | grep -qx '1' && grep -cF 'for dir in bindings/go/scip . reprolang swift; do' .github/workflows/ci.yaml | grep -qx '1' && grep -cF 'checks.x86_64-linux.swift' .github/workflows/ci.yaml | grep -qx '1' && grep -cF 'modRoot = "./swift";' checks.nix | grep -qx '1' && nix eval .#checks.x86_64-linux --apply builtins.attrNames --json | grep -F 'swift'</automated>
  </verify>
  <done>The swift module is visible to the CI fix job (paths-filter, tidy loop, nix-update attr), the checks.nix swift attribute builds green under nix flake check with a real vendorHash, the README is prettier-clean and documents runtime-only prerequisites, and flake.nix is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .scip index file → scip CLI / bindings | ParseSymbol consumes arbitrary, untrusted symbol strings from user-supplied index files; malformed input previously crossed into an out-of-bounds read (panic = DoS vector) |
| Repo → Go module proxy | swift/go.mod requires only already-pinned in-repo deps (scip bindings via replace, testify v1.11.1, rapid v1.3.0) — no new external packages in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Denial of Service | bindings/go/scip/symbol_parser.go peekNext | high | mitigate | The one-line guard fix returns unrecognizedDescriptorError instead of an OOB panic; regression inputs pinned in TestParseSymbolError; FuzzParseSymbol seed corpus keeps exploring the input space; full suite + -tags asserts + ./memtest prove no allocation/slot-reuse regression |
| T-01-02 | Tampering | namer output (swift/internal/symbol) | medium | mitigate | Every produced symbol is validated through scip.ParseSymbol before return (validate-before-use habit); a namer bug surfaces as an error, never as a malformed string reaching the index |
| T-01-03 | Repudiation | ci.yaml/checks.nix wiring | low | accept | Wiring drift is caught by nix flake check and the CI checks matrix; no untrusted input involved |
| T-01-SC | Tampering | package installs | n/a | accept | No npm/pip/cargo installs in this plan; Go module deps are in-repo or already pinned (testify, rapid) — outside the package-legitimacy gate's scope |

Blocking threshold: high (workflow.security_block_on). T-01-01 is the phase's one high-severity item and its mitigation IS Task 2. ASVS L1.
</threat_model>

<verification>
- Repo root: `go build ./... && go test ./...` green (workspace mode covers all four modules).
- bindings/go/scip: `go test ./... && go test -tags asserts ./...` green (includes ./memtest).
- swift/: `GOWORK=off go test ./...` green.
- `nix eval .#checks.x86_64-linux --apply builtins.attrNames --json` includes swift; `nix flake check` green (slow — run once at plan end).
- `nix develop --command gofmt -l swift bindings/go/scip` outputs nothing; `prettier --check swift/README.md` passes.
</verification>

<success_criteria>
- The walking-skeleton slice works: one real Swift symbol string round-trips through the bindings inside the new module (SKELETON.md capability).
- ROADMAP success criteria 3 and 4 hold: panic fixed + fuzz proof + all existing tests green; swift module builds/tests through go.work, the paths-filtered CI job, and nix flake check with nothing existing broken.
</success_criteria>

## Artifacts this phase produces (plan 01-01)

- New Go module: `github.com/scip-code/scip/swift` (swift/go.mod, swift/go.sum).
- New package: `github.com/scip-code/scip/swift/internal/symbol` — tracer-stage API: consts `Scheme`, `ManagerSwiftPM`; types `SymbolInput`, `Container`, `DeclKind` (minimal values DeclKindStruct, DeclKindMethod); func `Symbol(SymbolInput) (string, error)`.
- New package: `github.com/scip-code/scip/swift/cmd/scip-swift` (stub main; no installed binary yet).
- New test surface: `FuzzParseSymbol` fuzz target in bindings/go/scip (runs via go test; no new binaries installed).
- New Nix check attribute: `checks.x86_64-linux.swift` (buildGoModule, pname scip-swift).
- New files: swift/README.md, swift/cmd/scip-swift/main.go, swift/internal/symbol/namer.go, swift/internal/symbol/namer_test.go, bindings/go/scip/symbol_fuzz_test.go.
- Modified: go.work, go.work.sum, bindings/go/scip/symbol_parser.go (one guard line), bindings/go/scip/symbol_test.go (three appended inputs), .github/workflows/ci.yaml, checks.nix.

<output>
Create `.planning/phases/01-symbol-scheme-module-foundations/01-01-SUMMARY.md` when done
</output>
